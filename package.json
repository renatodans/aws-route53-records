{
  "name": "aws-route53-records",
  "version": "1.0.0",
  "description": "GitHub Action for AWS Route53 Records Sets",
  "main": "index.js",
  "scripts": {
    "build": "tsc && ncc build lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Renato Dans",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@aws-sdk/client-route-53": "^3.388.0",
    "@vercel/ncc": "^0.36.1"
  }
}
